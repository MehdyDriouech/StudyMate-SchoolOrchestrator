/**
 * Feature Catalog Library - Logique de la bibliothèque (STUB)
 */

export async function searchCatalog(query) {
  console.log('[Feature Catalog] Recherche dans le catalogue (STUB)', query);
  return { results: [] };
}

export default { searchCatalog };
